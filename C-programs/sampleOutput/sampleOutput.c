#include <stdio.h>

int main() {
  // declaring the num variable
  int num;
  printf("Enter a number: ");
  // taking input from user
  scanf("%d", &num);
  printf("You have entered %d", num);
  return 0;
}
